#!/usr/bin/env php
<?php

declare(strict_types=1);

/**
 * __DIR__ == .../<whmcs_root>/modules/<module_type>/<module_name>/vendor/aon4o/whmcs-helpers/bin
 *
 * 'autoload.php' is in 'vendor/' of your module.
 * 'init.php' is at the root of your WHMCS installation.
 */

// Require Composer's autoloader so we can use composer packages and our classes.
require_once __DIR__ . '/../../../autoload.php';

// Bootstrap WHMCS so we can use its functions and classes.
Aon4o\WhmcsHelpers\WHMCS::init();

(new Psy\Shell())->run();
